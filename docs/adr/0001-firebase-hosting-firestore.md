# Firebase Hosting + Firestore untuk v1, Auth ditunda

Memakai HTML statis + CSS kustom + Chart.js, di-host di Firebase Hosting, data mock di Firestore (`telemetry` per menit, `events` per siklus) agar nanti tinggal ganti source ke ESP32/laptop tanpa rombak UI; Auth tidak masuk v1 karena belum dibutuhkan untuk demo dosen.
